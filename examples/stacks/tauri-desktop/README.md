# Tauri desktop stack fixture

Exercises Atlas **DesktopGui** detection (Tauri + TypeScript frontend).

```bash
plx analyze examples/stacks/tauri-desktop
plx adapters desktop
```

Expected detections include Tauri, TypeScript, npm, and Vite-related tooling hints.
