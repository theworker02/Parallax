# FastAPI + SQLAlchemy stack fixture

Python stack manifest for `plx analyze` demos.

```bash
plx analyze examples/stacks/fastapi-sqlalchemy --to rust
plx mappings fastapi
plx mappings sqlalchemy
```

Expected detections include FastAPI, SQLAlchemy, Pydantic, pip/pyproject, Ruff, and mypy.
