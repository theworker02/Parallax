# Custom adapter example

Minimal illustration of `parallax-adapter-sdk` for a fictional `acme-router` framework detector.

This example is documentation-oriented: it compiles as a small crate depending on the SDK, and shows the contract Parallax expects.

See `src/lib.rs` and `adapter.toml`.
