# Parallax demo — capture this `state` binding and migrate to JavaScript.
state = {
    "username": "Ada",
    "score": 42,
    "projects": ["compiler", "runtime", "vm"],
}
print("python state ready:", state)
