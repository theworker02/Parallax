// Parallax demo — JavaScript side of the Python ↔ JS migration pair.
const state = {
  username: "Ada",
  score: 42,
  projects: ["compiler", "runtime", "vm"],
};
console.log("js state ready:", JSON.stringify(state));
